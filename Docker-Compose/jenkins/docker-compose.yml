---
version: "3.2"

services:
  agent:
    image: 127.0.0.1:5000/jenkins:lts
    ports:
      - "8080:8070"
      - "50000:50000"
    volumes:
      - /mnt/data/jenkins:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
#      - /var/lib/docker/volumes:/var/lib/docker/volumes
#    networks:
#      - agent_network
#    deploy:
#      mode: global
#      placement:
#        constraints: [node.platform.os == linux]

#  portainer:
#    image: portainer/portainer
#    command: -H tcp://tasks.agent:9001 --tlsskipverify
#    ports:
#      - "9000:9000"
#      - "8000:8000"
#    volumes:
#      - portainer_data:/data
#    networks:
#      - agent_network
#    deploy:
#      mode: replicated
#      replicas: 1
#      placement:
#        constraints: [node.role == manager]
##### portainerswarm
#networks:
#  agent_network:
#    driver: overlay
#volumes:
#  portainer_data:
#    driver: local
#    driver_opts:
#      type: nfs
#      o: addr=192.168.1.211,nolock,soft,rw
#      device: ":/mnt/user/Settings/Portainer-Cluster"


#services:
#  portainer:
#    image: portainer/portainer
#    container_name: znc
#    volumes:
#      - type: volume
#        source: nfs
#        target: /data
#        volume:
#          nocopy: true
#    ports:
#      - "9000:9000"
#    restart: unless-stopped
